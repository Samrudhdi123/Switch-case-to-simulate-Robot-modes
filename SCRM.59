#include <stdio.h>

int main() {
    float time, speed, distance;
    printf("Enter speed (m/s): ");
    scanf("%f", &speed);
    printf("Enter time (s): ");
    scanf("%f", &time);

    distance = speed * time;
    printf(" Distance Covered = %.2f meters.\n", distance);
    return 0;
}
