#include <stdio.h>

int main() {
    int battery;
    printf("Enter battery percentage: ");
    scanf("%d", &battery);

    if (battery >= 80)
        printf(" Battery Full\n");
    else if (battery >= 30)
        printf(" Battery Medium\n");
    else
        printf("Battery Low\n");

    return 0;
}
